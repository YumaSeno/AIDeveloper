services:
  ai_developer_ts:
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile
      args:
        CONTAINER_USER: vscode
        CONTAINER_WORKDIR: /workspace
    network_mode: "host"
    volumes:
      - ..:/workspace
    tty: true
    stdin_open: true
    privileged: true
